'''Implements the console entry point for :mod:`asyncutils`. Experimental (stick to ``asyncutils`` for now, but ensure you have the python scripts directory on PATH).
Run this with ``python [-m] asyncutils``.
The implementation file is executable on unix-like systems; simply input ``./asyncutils/__main__.py`` in the command prompt under the appropriate working directory.
Importing this file will cause :exc:`ImportError` to be thrown.'''