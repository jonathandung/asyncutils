'''Implements the console entry point for :mod:`asyncutils`.
The implementation file is executable on unix-like systems; simply input ``./asyncutils/__main__.py`` in the command prompt.
However, the preferred way to run the console is ``python -m asyncutils``, or simply ``asyncutils``.
Importing this file will cause :exc:`ImportError` to be thrown.'''