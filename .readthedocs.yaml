version: 2
sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true
build:
  os: ubuntu-26.04
  tools: {python: "3.14"}
  jobs:
    post_create_environment:
      - python3 -m pip install -e .[themes]
      - scripts/generate.sh
formats: [htmlzip, pdf]
